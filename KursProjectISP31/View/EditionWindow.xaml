<Window x:Class="KursProjectISP31.View.EditionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursProjectISP31.View"
        
        mc:Ignorable="d"
        Title="Добавление издания"
        Height="450" Width="400"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="50 20">
            <TextBlock Text="Выберите автора" Style="{StaticResource Text_Style}"/>
            <ComboBox x:Name="AuthorsComboBox"
                      DisplayMemberPath="Surname" SelectedValuePath="IdAuthor"
                      SelectedValue="{Binding IdAuthor}" />
        </StackPanel>
    </Grid>
</Window>
